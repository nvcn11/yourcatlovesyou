<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>your cat 🐱💖</title>
<style>
  /* GENERAL RESET */
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    height: 100vh;
    display: grid;
    place-items: center;
    font-family: 'Comic Sans MS', cursive, sans-serif;
    background: linear-gradient(135deg, #ffe6eb, #fff0f5);
  }

  .card {
    background: rgba(255,255,255,0.85);
    backdrop-filter: blur(10px);
    padding: 2rem 2.5rem;
    border-radius: 24px;
    text-align: center;
    box-shadow: 0 20px 40px rgba(0,0,0,0.08);
    width: 320px;
    position: relative;
  }

  h1 {
    font-size: 2rem;
    color: #ff4d6d;
    margin-bottom: 0.3rem;
  }

  p.subtitle {
    font-size: 1rem;
    color: #ff6b8a;
    margin-bottom: 1.5rem;
    opacity: 0.8;
  }

  /* CAT */
  .cat {
    font-size: 5rem;
    cursor: pointer;
    transition: transform 0.2s;
    display: inline-block;
    position: relative;
  }

  .cat:hover {
    transform: rotate(-10deg) scale(1.1);
  }

  .cat:active {
    transform: rotate(10deg) scale(1.2);
  }

  /* FLOATING HEARTS */
  .heart1, .heart2, .heart3, .heart4, .heart5 {
    position: absolute;
    font-size: 1.5rem;
    opacity: 0;
    animation: float 1.5s ease-out forwards;
  }

  .cat:hover .heart1 { animation-delay: 0s; opacity: 1; left: -10px; top: -50px;}
  .cat:hover .heart2 { animation-delay: 0.2s; opacity: 1; left: 20px; top: -40px;}
  .cat:hover .heart3 { animation-delay: 0.4s; opacity: 1; left: -25px; top: -45px;}
  .cat:hover .heart4 { animation-delay: 0.6s; opacity: 1; left: 15px; top: -55px;}
  .cat:hover .heart5 { animation-delay: 0.8s; opacity: 1; left: 0px; top: -60px;}

  @keyframes float {
    0% { transform: translateY(0); opacity: 1; }
    100% { transform: translateY(-80px); opacity: 0; }
  }

  /* STATUS MESSAGE */
  .status {
    margin-top: 1rem;
    font-size: 1rem;
    color: #ff4d6d;
    min-height: 1.5em;
  }

  /* CAT LEVEL-UP USING CHECKBOX */
  input[type="checkbox"] {
    display: none;
  }

  .cat-level {
    display: none;
  }

  input[type="checkbox"]:checked + .cat-level {
    display: block;
    font-size: 5rem;
    animation: bounce 0.6s infinite alternate;
  }

  @keyframes bounce {
    0% { transform: translateY(0); }
    100% { transform: translateY(-10px); }
  }

  /* FINAL VALENTINE MESSAGE */
  .valentine {
    display: none;
    font-size: 1rem;
    margin-top: 1rem;
    color: #ff4d6d;
    animation: fadeIn 1s forwards;
  }

  input[type="checkbox"]:checked ~ .valentine {
    display: block;
  }

  @keyframes fadeIn {
    0% {opacity: 0;}
    100% {opacity:1;}
  }

</style>
</head>
<body>

  <div class="card">
    <h1>Your Cat 🐱</h1>
    <p class="subtitle">Hover over it to see love!</p>

    <!-- INTERACTIVE CAT -->
    <label for="levelUp" class="cat">
      😺
      <span class="heart1">💗</span>
      <span class="heart2">💖</span>
      <span class="heart3">💗</span>
      <span class="heart4">💖</span>
      <span class="heart5">💗</span>
    </label>

    <!-- LEVEL-UP CHECKBOX (fake interaction) -->
    <input type="checkbox" id="levelUp">
    <div class="cat-level">😻</div>

    <div class="status">The cat is watching you lovingly…</div>

    <!-- FINAL VALENTINE MESSAGE -->
    <div class="valentine">
      💌 The cat says: "You make the world softer. Will you be my Valentine? 🐾💘"
    </div>

  </div>

</body>
</html>
